### **Main requirment of this**
- Teacher said the system must allow the dynamic minute template for each committees. That it the committee must be able to set the headers, languge and other things and must be able to edit it.
- Currently we are able to do it but in the simple box where it is asked. But for different meeting we might need sometimes to set the minute differently. Thus we need the big editor access to set the minute, and also the minute template for the commmittee.
- Teacher asked for the ai in the system for the mintues for roughyl written agendas and the decisons.
LLM_BASE_URL=https://api.xiaomimimo.com/anthropic
LLM_API_KEY=sk-
LLM_MODEL=mimo-v2.5-pro
The system should have a button to allow users to click use ai button then set rough prompt, and using the system prompt and the rough prompt should create the minute as described for meeting.
**Check**
- There is member list and the manage user section, the user in the manage user section are not reflected in the member list and vice versa.
- My intention was that person be allowed in the committee for the general member and he she be able to see the minutes of that assigned committee.